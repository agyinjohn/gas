# Moved to `apps/web`

The web app now lives at **`apps/web/`**. Use:

```bash
# From repo root
npm run dev:web

# Or from apps/web
cd apps/web && npm run dev
```

You can delete this `frontend/` folder once nothing is using it (e.g. stop any dev server still running from here).
